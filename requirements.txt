# Recall AI错题本 - 后端依赖
# 核心 Web 框架
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
python-multipart>=0.0.9

# 大模型（DeepSeek 兼容 OpenAI SDK）
openai>=1.30.0

# 向量数据库
chromadb>=0.4.24

# PDF 导出
reportlab>=4.0.0

# OCR（可选：缺失时自动降级为模拟识别，不影响启动）
# paddleocr>=2.7.0
# paddlepaddle>=2.6.0

# 开发辅助
python-dotenv>=1.0.0
